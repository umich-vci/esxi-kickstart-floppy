[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore:Unable to reliably determine FAT type:UserWarning:pyfatfs
markers =
    integration: marks tests that perform real filesystem I/O (e.g. write a floppy or parse an ISO)
